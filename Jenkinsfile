pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Deploy tn_service_registry build automatically #22'
                sh 'mvn --version'
            }
        }
    }
}
